trigger:
  branches:
    include:
      - main
  paths:
    include:
      - customer_api/*
      - k8s/customer-api/*

pool:
  vmImage: ubuntu-latest

variables:
  acrServiceConnection: 'sc-acr'
  aksServiceConnection: 'sc-aks'
  acrLoginServer: 'myacr.azurecr.io'
  imageRepository: 'customer-api'
  imageTag: '$(Build.BuildId)'
  namespace: 'customer'
  manifestPath: 'k8s/customer-api'

stages:
- stage: Build
  displayName: Build and Push Image
  jobs:
  - job: BuildAndPush
    steps:
    - task: Docker@2
      displayName: Build and push customer-api
      inputs:
        command: buildAndPush
        repository: $(imageRepository)
        dockerfile: customer_api/Dockerfile
        containerRegistry: $(acrServiceConnection)
        tags: |
          $(imageTag)
          latest

- stage: Deploy
  displayName: Deploy to AKS
  dependsOn: Build
  jobs:
  - deployment: DeployCustomerApi
    displayName: Deploy customer-api
    environment: aks-dev
    strategy:
      runOnce:
        deploy:
          steps:
          - task: KubernetesManifest@1
            displayName: Deploy manifests
            inputs:
              action: deploy
              kubernetesServiceConnection: $(aksServiceConnection)
              namespace: $(namespace)
              manifests: |
                $(manifestPath)/configmap.yaml
                $(manifestPath)/deployment.yaml
                $(manifestPath)/service.yaml
                $(manifestPath)/ingress.yaml
              containers: |
                $(acrLoginServer)/$(imageRepository):$(imageTag)